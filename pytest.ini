[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --tb=short
markers =
    unit: testy jednostkowe
    integration: testy integracyjne
    regression: testy regresyjne
    api_live: testy na żywo wymagające kluczy API (Serper, Anthropic)
